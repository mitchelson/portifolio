# Optional: override the public site URL used for absolute asset references at build time.
# Defaults are set in src/config/seo.ts (Vercel production domain).
VITE_SITE_URL="https://portifolio-rho-sand-47.vercel.app"
